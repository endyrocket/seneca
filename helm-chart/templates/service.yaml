apiVersion: v1
kind: Service
metadata:
  name: {{ include "seneca.fullname" . }}
  labels:
    {{- include "seneca.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "seneca.selectorLabels" . | nindent 4 }} 